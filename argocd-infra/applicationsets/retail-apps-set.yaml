apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: retail-microservices
  namespace: argocd
spec:
  generators:
    - git:
        repoURL: 'https://github.com/Chinthaparthy-UmasankarReddy/5-tier-retail-infra-gitops.git'
        revision: dev
        directories:
          - path: apps/microservices/*
  template:
    metadata:
      name: '{{path.basename}}-app'
    spec:
      project: default
      sources:
        # Source 1: The Core Helm Chart Template Engine
        - repoURL: 'https://github.com/Chinthaparthy-UmasankarReddy/5-tier-retail-infra-gitops.git'
          targetRevision: dev
          path: apps/central-shared-chart
          helm:
            valueFiles:
              - '$git-repo/apps/microservices/{{path.basename}}/values.yaml'
        # Source 2: The Environmental Configurations Pointer
        - repoURL: 'https://github.com/Chinthaparthy-UmasankarReddy/5-tier-retail-infra-gitops.git'
          targetRevision: dev
          ref: git-repo
      destination:
        server: 'https://kubernetes.default.svc'
        namespace: production
      # Note: syncPolicy is explicitly aligned for multi-source array blocks here
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true